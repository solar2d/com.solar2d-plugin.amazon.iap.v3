Sources for the plugin `plugin.amazon.iap.v3`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.amazon.iap.v3" = {
            publisherId = "com.solar2d",
        },
    },
}
```
